# R Markdown

This folder contains the R Markdown source for the GIS-enhanced forecasting workflow.

Run `source("../requirements.R")` from this folder or `source("requirements.R")` from the repository root before knitting `WOMart-GIS-Sales-Forecasting.Rmd`.
