# R Markdown

This folder contains the R Markdown source for the modeling workflow, exploratory plots, and Shiny dashboard.

Run `source("../requirements.R")` from this folder or `source("requirements.R")` from the repository root before knitting the document.
